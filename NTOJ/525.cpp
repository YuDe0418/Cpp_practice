#include <bits/stdc++.h>
using namespace std;

signed main(void)
{
    int p,q; cin >> p >> q;
    cout << (p+q)/2 << "\n";
    // int p,q; cin >> p >> q;
    // int n = abs(p-q) - 1;
    // int num = min(p,q) + 1;
    // for(int i=0;i<n;i++)
    // {
    //     if(abs(abs(num-p) - abs(num-q)) <= 1) 
    //     {
    //         cout << num << "\n"; 
    //         break;
    //     }
    //     else num++;
    // }
}